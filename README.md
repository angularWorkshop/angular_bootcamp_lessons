# UI States with @if

This branch contains the completed solution for the conditional rendering exercise.

## What the solution demonstrates

- `@if (isLoading)` / `@else if (hasError)` / `@else if (products.length === 0)` / `@else`
- only one UI state is visible at any time
- state transitions work correctly through button clicks

## Notes

- This is the `answer` branch — reference implementation.
- All tests should pass.

## Commands

```bash
npm start
npm test -- --runInBand
```
