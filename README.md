# Runtime config and telemetry

## Goal

Connect runtime flags, environment metadata, and telemetry into one feature contract.

## What you are training

- runtime feature flags
- telemetry events
- error instrumentation

## Task

Finish the TODOs in the starter files.

1. hide the action when the flag is disabled
2. capture the action event
3. capture the error event

## Definition of done

- all tests pass with `npm test -- --runInBand`
- `npm run start:dev` shows a working Angular screen and a green Jest watcher
