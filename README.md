# Deferred secondary workspace

## Goal

Move the secondary block under a real defer boundary and keep the primary path visible first.

## What you are training

- defer block syntax
- placeholder behavior
- screen budget status

## Task

Finish the TODOs in the starter files.

1. switch the secondary block from a plain conditional to a defer block
2. keep the placeholder path working

## Definition of done

- all tests pass with `npm test -- --runInBand`
- `npm run start:dev` shows a working Angular screen and a green Jest watcher
