# Accessible form errors

## Goal

Build a form flow where validation feedback, aria attributes, and the next focus target stay coordinated.

## What you are training

- accessible form validation
- live region updates
- focus-first-error behavior

## Task

Finish the TODOs in the starter files.

1. finish the submit flow
2. link field errors through aria-describedby
3. update the live region and first invalid field id

## Definition of done

- all tests pass with `npm test -- --runInBand`
- `npm run start:dev` shows a working Angular screen and a green Jest watcher
