# Realtime feed with reconnect status

## Goal

Keep the feed data and the connection indicator synchronized as one realtime screen contract.

## What you are training

- realtime feed state
- reconnect status
- explicit connection modes

## Task

Finish the TODOs in the starter files.

1. append new events to the feed
2. react to disconnect and reconnect
3. keep the connection indicator honest

## Definition of done

- all tests pass with `npm test -- --runInBand`
- `npm run start:dev` shows a working Angular screen and a green Jest watcher
