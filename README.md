# Operations platform hardening

## Goal

Add SSR-safe guards, accessibility feedback, a budget indicator, and release telemetry to the operations shell.

## What you are training

- SSR-safe browser access
- a11y live messages
- release readiness
- telemetry event capture

## Task

Finish the TODOs in the starter files.

1. guard browser-only storage access
2. update the live region when accessibility becomes ready
3. show the extended budget state
4. mark telemetry as ready and unlock the release checklist

## Definition of done

- all tests pass with `npm test -- --runInBand`
- `npm run start:dev` shows a working Angular screen and a green Jest watcher
