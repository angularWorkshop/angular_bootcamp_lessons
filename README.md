# Track and render budget

## Goal

Use computed state and a stable track expression to keep the list predictable under a filter change.

## What you are training

- computed derived state
- track by id
- template-level performance guard

## Task

Finish the TODOs in the starter files.

1. compute the visible rows from the source list and filter
2. replace the unstable track expression with a stable id

## Definition of done

- all tests pass with `npm test -- --runInBand`
- `npm run start:dev` shows a working Angular screen and a green Jest watcher
