# Shell library components

## Goal

Build a reusable shell-card component with inputs, an output action, and projection slots.

## What you are training

- shared component API
- projection slots
- internal action output

## Task

Finish the TODOs in the starter files.

1. apply the tone to the host card
2. render the projected body and action slot
3. emit the action event from the shared button

## Definition of done

- all tests pass with `npm test -- --runInBand`
- `npm run start:dev` shows a working Angular screen and a green Jest watcher
