# Focus restore for filters and tables

## Goal

Make the focus route predictable when the user opens, applies, or cancels the filters panel.

## What you are training

- focus restore
- live region updates
- table navigation context

## Task

Finish the TODOs in the starter files.

1. open the filters panel
2. apply the filters and restore the results context
3. cancel and return focus to the reopen button

## Definition of done

- all tests pass with `npm test -- --runInBand`
- `npm run start:dev` shows a working Angular screen and a green Jest watcher
