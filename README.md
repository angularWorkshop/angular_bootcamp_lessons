# Workspace boundaries and shared contracts

## Goal

Remove forbidden cross-layer imports and keep shared/core code dependent on contracts instead of feature services.

## What you are training

- workspace boundaries
- shared contracts
- architecture-level code checks

## Task

Finish the TODOs in the starter files.

1. remove forbidden imports from shared and core files
2. keep the UI rendering through the shared view model contract

## Definition of done

- all tests pass with `npm test -- --runInBand`
- `npm run start:dev` shows a working Angular screen and a green Jest watcher
